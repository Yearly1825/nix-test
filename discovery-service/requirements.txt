# Simplified Discovery Service - Minimal Dependencies
# Core dependencies for simplified implementation

fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.5.0
cryptography==41.0.7
pyyaml==6.0.1

# Dependencies for HTTP requests and NTFY notifications
requests==2.31.0
httpx==0.25.2

# Removed dependencies (available in original requirements.txt):
# - slowapi (rate limiting removed)
# - python-multipart (not needed for simplified endpoints)
# - pydantic-settings (using manual config loading)
